- Initialized the npm package manifest with ESM module type, build scripts, and a Node 20 engine constraint
- Added runtime dependencies with versions reconciled against the monorepo's hoisted majors for twilio, sendgrid, slack, and bullmq
- Declared mjml explicitly rather than relying on the workspace hoisting the source service had silently depended on
- Configured strict TypeScript with NodeNext resolution and no path aliases, avoiding the source repo's import-rewriting hack
- Added the eslint configuration with typescript rules plus a guard banning environment reads outside the config module
- Configured jest for ESM with the ts-jest transform and module name mapping for extensionful relative specifiers
- Added the multi-stage production Dockerfile and a local compose stack running postgres and redis on isolated ports
- Added the GitHub Actions workflow running lint, typecheck, tests, and a docker build on every push and pull request
- Created the migrations directory with an operator contract README forbidding automated or agent-driven execution
- Added a placeholder express entrypoint serving a liveness health endpoint so the toolchain could be validated end to end
- Vendored the database client from shared-libs, dropping the RDS Data API branch and the cross-service global pool
- Vendored the tenant scoping helper and generalized its medspa and location predicates into tenant and sub-tenant scoping
- Vendored the redis cache and client with a configurable key prefix and graceful degradation when redis was unreachable
- Vendored the observability logger, metrics registry, and request middleware under the new outreach metric name prefix
- Vendored the gateway auth middleware and generalized its identity headers while continuing to accept the legacy medspa header names
- Introduced the auth mode seam with gateway trust implemented and the api-key and jwt strategies stubbed for later phases
- Replaced the Mentera-specific permission enum with outreach-scoped permissions covering send, approve, configure, and admin
- Centralized every environment variable read behind a zod-validated config module that failed fast during service boot
- Added the typed error hierarchy and a terminal handler that logged request identifiers and hid stack traces in production
- Wired the composition root, mounting metrics and mcp before authentication to preserve the source service's ordering constraints
- Added liveness and detailed readiness endpoints checking database, redis, and queue directly rather than via an external monitor
- Covered tenant scoping branches, identity header precedence, and the boot failure raised by a missing database connection string
- Recorded the upstream commit SHA for every vendored file so future drift against shared-libs could be reasoned about
- Defined the tenancy tables for tenants, sub-tenants, api keys, and per-tenant channel credential storage
- Defined recipients, consent records, and cached recipient context, giving the engine its own first-class identity concept
- Defined recipient preferences with per-playbook opt-outs and an explicit quiet-hours timezone column
- Defined messages and outreach events with nullable recipient and sender columns so staff-directed sends became representable
- Defined templates and template versions as the union of the two previously divergent owner definitions
- Defined prompt packs, assets, notifications, analytics, scheduled messages, and the model interaction audit table
- Defined packs, tenant pack installations, playbooks, and playbook triggers, replacing the hardcoded event type enum
- Defined approvals and approval policies, replacing approval state previously buried inside a jsonb column
- Defined campaigns, audiences, audience members, and batch tracking tables for the orchestrator built later
- Authored the idempotent core schema migration with pgcrypto, timestamptz columns, and fully guarded statements
- Authored the approvals and playbooks migrations with state check constraints and approver lookup indexes
- Ported the production performance indexes covering conversation, unread, and tenant-scoped message access patterns
- Asserted tenant column presence, timestamptz usage, approval state constraints, and full migration idempotency
- Defined the channel port with capability declarations, validation, credential injection, and delivery result contracts
- Implemented the channel registry, replacing the six-case dispatch switch with typed lookup and explicit unknown-channel errors
- Generalized the Twilio three-level credential fallback into a resolver serving every channel uniformly
- Cached resolved channel credentials in redis with invalidation triggered by configuration writes
- Ported the SendGrid adapter and returned the provider message identifier the source service had previously discarded
- Ported the SMTP fallback adapter, preserving nodemailer transport options and the oauth service account path
- Ported the Twilio adapter with a bounded client cache and dry-run behavior driven by explicit configuration
- Ported the Slack adapter as a pure send surface, relocating urgent-alert and appointment helpers into pack code
- Ported the push, webhook, and in-app adapters onto the shared channel interface with correct capability declarations
- Ported the notification queue to bullmq version five with a uniform job payload and registry-driven dispatch
- Preserved retry semantics exactly, including ten attempts for urgent priority and the existing cleanup windows
- Added the dispatcher entry point performing capability validation and carrying a marked hook for the compliance gate
- Ported the tenant and agent channel configuration services onto the renamed credential tables
- Emitted per-channel send counters and latency histograms tagged by tenant, channel, and terminal delivery status
- Covered every credential fallback level, retryable error classification, and end-to-end queue delivery with backoff
- Defined the llm provider port with schema-constrained json generation, token accounting, and latency reporting
- Ported the Bedrock client into a provider adapter, preserving multi-model routing and the existing request timeout behavior
- Replaced the hourly metrics interval with prometheus gauges and persisted every call to the model interaction audit table
- Defined the template store port covering crud, versioning, usage counting, and default template selection
- Split the template renderer into handlebars compilation, helper registration, and mjml conversion modules
- Implemented the template store, preserving the default-clearing invariant the providers-service settings screen relied on
- Ported ai template generation and prompt enhancement onto the llm provider port rather than a bespoke client
- Ported asset upload and image generation with configurable s3 or local filesystem storage backends
- Introduced the generic render context and pack alias map so existing medspa templates rendered without being rewritten
- Extracted the inlined healthcare prompts out of generator code into versioned medspa prompt pack data files
- Implemented deterministic prompt assembly ordering persona, goal, constraints, validated context, and channel rules
- Implemented draft generation with zod contract validation and a confidence score documented as heuristic rather than calibrated
- Exposed the versioned content generation, template crud, rendering, and asset management endpoints
- Added golden prompt assembly assertions and verified contract violations short-circuited before any model invocation occurred
- Defined the context provider port with caller-supplied inline data as the default first-class resolution path
- Implemented the inline provider and a registry scoping resolution to the packs a tenant had actually installed
- Isolated every Mentera service call into a single pack-registered patient and provider context adapter
- Cached provider results with a configurable ttl while preserving partial-failure tolerance on health insight lookups
- Implemented recipient upsert, batch lookup, and search, retaining the empty-identifier guard that had prevented an inbox outage
- Replaced cross-database patient name lookups with recipient records the engine now owned outright
- Ported the preference engine onto database storage, removing the process-local map that lost state on every restart
- Preserved cross-midnight quiet hours evaluation with explicit per-recipient timezone resolution and the urgent priority override
- Implemented the ordered suppression gate covering status, consent, opt-outs, quiet hours, rate limits, and throttles
- Recorded suppressed and deferred messages as first-class rows instead of dropping them silently
- Added shadow mode so the gate reported would-suppress telemetry while messages continued to be delivered
- Implemented data-driven content lint with pack-level prohibited phrases, disclaimers, and link policies
- Added the dry-run retention purge job driven by the per-tenant retention setting nothing had previously read
- Exposed recipient, preference, consent, and token-based unsubscribe endpoints with appropriate rate limiting
- Covered quiet hours across daylight-saving boundaries, gate precedence ordering, and preference durability across restarts
- Implemented the canonical ten-state machine as a pure table-driven transition function with no side effects
- Appended immutable audit entries recording actor, transition, reason, and content hash for every decision
- Implemented the policy engine covering always, threshold, deterministic sample, and none decision modes
- Implemented approver resolution for agent, named role, group quorum, and stable round-robin strategies
- Gated threshold auto-approval behind explicit per-tenant opt-in given the heuristic nature of the confidence score
- Implemented the approval service with message-scoped idempotency and the sender ownership check carried from the source
- Routed approved messages through the dispatcher so the compliance gate still ran after human sign-off
- Implemented bulk actions gated on both caller permission and the policy's declared bulk approval rights
- Added the sla worker applying escalation, auto-decline, or auto-approve when a review deadline expired
- Seeded the provider-always and system-transactional baseline approval policies shipped with the engine
- Exposed the versioned approval listing, dashboard, decision, edit, schedule, and policy management endpoints
- Asserted exhaustive transition legality, cross-sender authorization denial, and idempotent repeated approval calls
- Verified an unsubscribed recipient yielded suppression rather than delivery even after a human approved the draft
- Defined the outreach trigger contract normalizing http events, schedules, campaign launches, and inbound webhooks
- Implemented trigger matching with a deliberately bounded predicate vocabulary and deterministic priority ordering
- Implemented the runtime resolving recipient, context, channel, content, and approval for each matched playbook
- Enforced data contract validation and idempotency keys so a redelivered event could never produce a duplicate send
- Implemented pack installation, playbook crud, and per-tenant activation with idempotent upsert semantics
- Added the pack manifest format and a validating loader rejecting malformed definitions with readable errors
- Authored the seventeen medspa playbooks transcribed directly from the retired event handler switch statement
- Added trigger aliases reconciling the mismatched event names between the source enum and its handler switch
- Derived medspa data contracts as zod schemas, making defensively-read fields optional with explicit defaults
- Moved the hardcoded emergency recipient address out of handler code into per-tenant pack configuration
- Seeded onboarding, farewell, follow-up, promotion, and feedback playbooks replacing the undeclared rule tables
- Implemented the data-driven ehr event mapper, replacing the bespoke mapping service with pack configuration
- Wired the event queue processor into the runtime and persisted playbook run outcomes for observability
- Asserted medspa parity on template selection, channel set, recipient, and approval requirement for every event
- Defined the storage port and implemented local-filesystem and S3 adapters behind it, wiring config declared since scaffold
- Assigned asset storage keys from the tenant and a uuid rather than the caller's filename, closing an arbitrary-write path
- Recorded every uploaded asset against its tenant, giving the assets table its first writer and its first tenant boundary
- Declared the image generation port and registered no adapter, since the source's image method threw unconditionally
- Ported multimodal generation, template-with-images, and the four campaign template endpoints off their not-implemented responses
- Added structured generation to the content generator for output that is a composition plan rather than a message body
- Shipped the core template-author, campaign-author, and multimodal prompt packs, including one an endpoint had defaulted to since the compat shim
- Moved the campaign prompt's vertical vocabulary out of engine code and into pack content
- Implemented api-key authentication against the tenant key table, taking tenant, permissions, and role from the key row alone
- Hashed api keys with sha256 rather than a salted password hash, making the unique-index lookup possible
- Added key rotation with an overlap window and a fixed-window per-key rate limiter backed by redis
- Added per-tenant usage reporting over model spend and delivery volume, and recorded the token split the audit row dropped
- Validated a pack's required configuration at install time and merged tenant config instead of replacing it
- Added envelope encryption for channel credentials with key rotation, sealing on write and clearing the plaintext column
- Wrote the credential decommissioning migration behind a guard refusing to run while any credential is unsealed
- Added the one-shot credential backfill script, reporting by default and writing only when asked
- Separated decommissioning migrations from baseline schema and gave every harness one shared definition
- Promoted the compliance profile column into an enforced ruleset with can-spam and tcpa as non-optional defaults
- Deferred marketing texts outside the statutory window in the recipient's own timezone rather than dropping them
- Blocked content flagged as protected health information on channels whose transport the service does not control
- Ran the content linter inside the compliance gate so a caller cannot disable a rule by omission
- Added recipient erasure that removes the person without removing the record that a message was sent
- Retained consent records through erasure, with the reason returned in the response rather than left implicit
- Added the portability export, served as an attachment and reporting truncation instead of silently stopping
- Rejected campaign playbooks omitting the targeting predicate, which until now fired on every campaign a tenant ran
- Resolved role membership when authorizing an approval instead of accepting any holder of the approve permission
- Failed closed on unconfigured roles and on a missing authorization provider rather than falling back to the permission
- Cancelled everything the migration left in flight, once, after the source stopped rather than on every delta
- Kept the original status word on every migrated message so cancelling the backlog stayed answerable
- Asserted in the verifier that nothing migrated is still actionable, as a no-op until finalize has run
- Rewrote the cutover for a product with no traffic, no staging and no parallel run
- Moved credential encryption into the cutover window, since nothing re-inserts plaintext after the single load
- Recorded the assumption that was wrong and what it had been protecting against
- Trimmed the legacy surface to the endpoints six consumers actually reach, established by inspection
- Answered retired legacy mounts with a gone status naming the successor rather than a bare not-found
- Kept the provider and ehr callback mounts without proof of use, since their urls live outside the repository
- Extracted generate-then-review out of the compat shim into an engine service with three callers
- Added the versioned outreach generation route two retired legacy mounts had been naming as their successor
- Left the approval policy unnamed so a draft falls to the review-everything default rather than a mode that dispatches
- Refused to invent a recipient or a contact point, rather than paying for a model call and failing at dispatch
- Added the five review tools that give Tera drafting, approval, conversation and campaign capability
- Declared each tool's mutation flag beside the tool and published the set on discovery
- Corrected the orchestrator's confirmation gate, which named a slack tool that has never existed
- Warned when a gate entry matches no discovered tool, since a stale name fails open and looks identical to a working one
- Classified the new tools in the permission catalog, where a missing entry fails closed without saying so
- Removed the legacy approval blob column from the baseline schema rather than shipping it to be dropped
- Stopped the data migration writing a column no reader remains for
- Held the legacy envelope's four approval fields as constants, since both apps still read them
- Kept the pending-approval count structurally zero, rather than changing a rendered number inside a cleanup
- Distinguished a queue that would not take the job from a decision that the message should not go
- Kept the approval standing when the send failed for an infrastructural reason
- Marked an unqueued message failed rather than leaving it in the state that means something will collect it
- Retried the release on a second approval instead of reporting work already done
- Cleared the retry marker on the one path that proves the queue accepted the job
- Forwarded the generic tenancy headers from the gateway alongside the ones every other service reads
- Sent both spellings from the orchestrator so one code path serves all three services
- Read only the generic tenancy headers in the engine, removing the last vertical noun from the wire protocol
- Rejected a caller that sends only the legacy header rather than serving it against an empty tenant
- Inverted the tests that asserted the alias worked, so restoring it fails
- Kept the sender alias, which is not the tenancy boundary and whose callers were not surveyed
- Kept the outbound medspa headers on the context adapter, which targets services that read only those
